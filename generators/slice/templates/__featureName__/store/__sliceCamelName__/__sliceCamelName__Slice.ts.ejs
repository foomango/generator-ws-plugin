import { createSlice, PayloadAction } from '@reduxjs/toolkit'

import { FEATURE_PREFIX } from '../../prefix'
import { <%= sliceName %>State } from './<%= sliceName %>State'

const <%= sliceCamelName %>Slice = createSlice({
  name: `${FEATURE_PREFIX}.<%= sliceName %>`,
  initialState: { todo: 'todo' } as <%= sliceName %>State,
  reducers: {
    update<%= sliceName %>(state, action: PayloadAction<<%= sliceName %>State>) {
      return { ...state, ...action.payload }
    },
  },
})

const { actions, reducer } = <%= sliceCamelName %>Slice

export const { update<%= sliceName %> } = actions

export default reducer
