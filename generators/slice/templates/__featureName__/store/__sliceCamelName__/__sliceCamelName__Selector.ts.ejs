import { createSelector } from '@reduxjs/toolkit'

import { select<%= parentReducerName %> } from '../<%= featureCamelName %>/<%= parentReducerCamelName %>Selector'

export const select<%= sliceName %> = createSelector(
  select<%= parentReducerName %>,
  (<%= parentReducerCamelName %>) => <%= parentReducerCamelName %>.<%= sliceCamelName %>
)
