import React from 'react'

import { connector, PropsFromRedux } from './<%= featureCamelName %>EntryConnector'
import { FeatureComponentProps } from '../../../migration'

export const <%= featureName %>Entry: React.FC<
  PropsFromRedux & FeatureComponentProps
> = ({ updateUi, ui }) => {
  window.console.log('ui:', ui)
  React.useEffect(() => {
    updateUi({ todo: 'foo' })
  }, [])

  return <div>Todo:</div>
}

export default connector(<%= featureName %>Entry)
